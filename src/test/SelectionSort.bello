n=readInt();

arr=newArray(n);

for (i=0;i<n;i++)
{
    arr[i]=readInt();
}


function selectionSort(a, lnth)
{
    var tmp;

    var tmpPnt;

    tmpPnt=0;

    var i,j;

    println("sort\n");

    for (i=0;i<lnth;i++)
    {
        tmpPnt=i;
        for (j=i;j<lnth;j++)
        {
            if (a[j]<a[tmpPnt])
            {
                tmpPnt=j;
            }
        }

        tmp=a[tmpPnt];
        a[tmpPnt]=a[i];
        a[i]=tmp;

    }
}

selectionSort(arr,n);

print(arr);