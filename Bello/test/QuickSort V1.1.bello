n=readInt();

arr=newArray(n);

for (i=0;i<n;i++)
{
    arr[i]=readInt();
}


function quickSort(a, l, r)
{
    //print("quickSort function");

    if (l>=r)
    {
        return;
    }

    var tmp;

    if (l+1==r)
    {
        if (a[l]>a[r])
        {
            tmp=a[l];
            a[l]=a[r];
            a[r]=tmp;
        }

        return;
    }

    var pvt;

    pvt=a[l];

    //print("pvt:"+pvt);

    var lp, rp;

    lp=l+1;

    rp=r;

    

    var i;

    i=0;

    //print("initial lp: "+lp+ " rp:"+rp);

    while (lp<=rp)
    {
        if (a[lp]>pvt)
        {
            tmp= a[lp];
            a[lp]=a[rp];
            a[rp]=tmp;
            rp--;
            //print("prcs #0"+" lp:"+lp+" rp:"+rp);

        }
        else if (a[rp]<=pvt)
        {
            tmp = a[rp];
            a[rp]=a[lp];
            a[lp]=tmp;
            lp++;
            //print("prcs #1"+" lp:"+lp+" rp:"+rp);
        }
        else 
        {
            lp++;
            rp--;
            //print("prcs #2"+" lp:"+lp+" rp:"+rp);
        }

        i++;
        //print("i: "+i);
        
        //print(a);
    }


    tmp=a[l];
    a[l]=a[rp];
    a[rp]=tmp;

    //print("finish turn lp: "+lp+ " rp:"+rp);
    //print(a);

    //print("before rp>l: "+lp+ " rp:"+rp);
    if (rp>l) 
    {
        //print("rp>l");
        quickSort(a,l,rp);
    }

    //print("before lp<r: "+lp+ " rp:"+rp);

    if (lp<r)
    {
        //print("lp<r");
        quickSort(a,lp,r);
    }
    

    return;
}

quickSort(arr,0,n-1);

print(arr);